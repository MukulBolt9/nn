<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nowbrief">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- Samsung Live Notifications support -->
        <meta-data
            android:name="com.samsung.android.support.ongoing_activity"
            android:value="true" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- NowBrief Dedicated Activity (Samsung Now Bar) -->
        <activity
            android:name=".activities.NowBriefActivity"
            android:label="NowBrief"
            android:theme="@style/NowBriefTheme"
            android:launchMode="singleTask"
            android:exported="true"
            android:taskAffinity="com.nowbrief.nowbar"
            android:excludeFromRecents="false"
            android:showOnLockScreen="true">
            <intent-filter>
                <action android:name="com.nowbrief.OPEN_NOWBRIEF" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Foreground Service for Live Notifications -->
        <service
            android:name=".services.NowBriefLiveService"
            android:foregroundServiceType="dataSync"
            android:exported="false"
            android:enabled="true" />

        <!-- Boot Receiver -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Notification Action Receiver -->
        <receiver
            android:name=".receivers.NotificationActionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.nowbrief.ACTION_DISMISS" />
                <action android:name="com.nowbrief.ACTION_OPEN" />
                <action android:name="com.nowbrief.ACTION_NEXT" />
            </intent-filter>
        </receiver>

        <!-- Alarm Receiver for scheduled updates -->
        <receiver
            android:name=".receivers.AlarmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.nowbrief.ACTION_UPDATE" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
